@model IEnumerable<MVCLayer.Models.ClientViewModel>

@{
    ViewBag.Title = "Clients";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Clients</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nickname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nickname)
        </td>
        @if (ViewBag.IsAdmin)
        {
            <td>
                @Html.ActionLink("Edit", "EditClient", new { controller="Editsales", id = item.Id })
            </td>
        }
    </tr>
}

</table>
